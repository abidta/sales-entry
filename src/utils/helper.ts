// const nameArr: string[] = [
//     `${prefix}.${index}.sr_no`,
//     `${prefix}.${index}.item_code`,
//     `${prefix}.${index}.item_name`,
//     `${prefix}.${index}.qty`,
//     `${prefix}.${index}.rate`,
//     `${prefix}.${index}.amount`,
//   ];
// export const  validation=(name:string):boolean=>{

// return true
// }